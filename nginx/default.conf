server {
    listen 80;

    # Serve the files from the specified directory
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Log files
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
}
